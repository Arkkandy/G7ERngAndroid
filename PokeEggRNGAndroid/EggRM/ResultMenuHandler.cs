using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;

namespace Gen7EggRNG.EggRM
{
    public class ResultMenuHandler
    {
        private SearchParams searchParams;

        public ResultMenuHandler(SearchParams sp ) {
            searchParams = sp;
        }

        public void SetMenuHandler() {
            if (searchParams.type == SearchType.NormalSearch) {

            }
            else if (searchParams.type == SearchType.NormalSearch)
            {

            }
            else if (searchParams.type == SearchType.NormalSearch)
            {

            }
            else if (searchParams.type == SearchType.NormalSearch)
            {

            }
        }
    }
}